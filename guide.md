# 프로필 카드 만들기 — 실습 가이드

> 오늘은 나만의 프로필 카드를 **3단계에 걸쳐 조금씩 발전시키며** 완성합니다. 매 단계마다 무엇이 추가됐는지 이해하고 넘어가는 게 핵심입니다. 또한 오늘 처음으로 `dev`, `gh-pages`라는 브랜치를 실제로 사용해봅니다.

## 🎯 오늘의 목표

1. Gemini로 프로필 카드를 단계별로 발전시켜 완성한다.
2. `main`(보관) / `dev`(작업) / `gh-pages`(배포) 브랜치를 구분해서 사용한다.
3. 완성한 코드를 Gemini가 직접 설명해주는 학습 문서를 만든다.
4. 태그와 Release Note를 처음 사용해본다.

> ⚠️ 이메일, 전화번호 등 개인 연락처는 어디에도 넣지 마세요. 이름과 SNS 링크는 공개해도 괜찮습니다.

---

## 1️⃣ 이 저장소 Fork & Clone

1. 이 저장소(`hitech26-week3-profilecard`) **Fork** → 이름을 `profilecard`로 변경 → **Public** 유지
2. 로컬로 Clone

```bash
cd Desktop
git clone https://github.com/본인계정명/profilecard.git
cd profilecard
code .
```

## 2️⃣ 작업용 브랜치(`dev`) 만들기

오늘 모든 작업은 `main`이 아니라 `dev`라는 브랜치에서 진행합니다. `main`은 원본 그대로 보관해두는 용도입니다.

```bash
git checkout -b dev
```

터미널 또는 VS Code 좌측 하단에 `dev`라고 표시되면 브랜치 전환이 잘 된 것입니다.

---

## 3️⃣ 사이클 1 — 기본 프로필 카드 생성

Gemini에 아래처럼 요청합니다.

```
반응형 개인 프로필 카드를 만들어줘.
- 프로필 사진(원형), 이름, 한 줄 소개, SNS 아이콘 3개(GitHub, LinkedIn, Instagram)
- 모바일에서도 깨지지 않게 반응형으로
- HTML, CSS, JS 파일을 각각 분리해서 줘 (index.html, style.css, script.js)
- 색상은 [원하는 색 계열]로
```

받은 코드를 `index.html`, `style.css`, `script.js`로 저장하고, 브라우저(또는 Live Server)로 미리보기 합니다.

### 잠깐, 코드 읽어보기

전체를 다 이해할 필요는 없지만, 아래 정도는 찾아보세요.

- [ ] `<div>`와 `class`가 어디에 몇 번 쓰였는지 (묶고 이름 붙이는 역할)
- [ ] CSS에서 색상 값(`#`으로 시작하는 코드)이 어디 있는지
- [ ] `@media`로 시작하는 부분이 어디 있는지 (화면 크기별로 다르게 보이도록 하는 부분)

다 확인했으면 커밋합니다.

```bash
git add .
git commit -m "사이클1: 기본 프로필 카드 생성"
```

---

## 4️⃣ 사이클 2 — 링크 버튼 섹션 추가 + 내 정보 채우기

이번엔 참고할 스타일 이미지(리틀리 같은 링크 허브 서비스 스크린샷)를 Gemini에 함께 보여주며 요청합니다.

```
[참고할 스크린샷 이미지 첨부]
이 이미지처럼, 프로필 카드 아래에 링크 버튼 목록을 추가해줘.
버튼 2~3개 (예: 블로그, 포트폴리오 등 원하는 항목으로)
클릭하면 각 링크로 이동하게 만들어줘.
```

받은 코드를 반영하고, 새로 추가된 부분을 살펴봅니다.

- [ ] 버튼 여러 개가 비슷한 코드 구조가 반복되는 형태인지 확인

### 내 정보로 직접 바꾸기

이번엔 Gemini에게 요청하지 않고, **VS Code에서 직접** 아래 값을 내 정보로 바꿔봅니다.

- 프로필 이미지 경로(본인 사진 파일 경로 또는 이미지 URL)
- 이름, 소개 문구
- SNS 링크 실제 주소 (GitHub, LinkedIn 등)

> 💡 모든 수정을 AI에게 요청할 필요는 없습니다. 텍스트나 링크처럼 단순한 값은 직접 바꾸는 게 더 빠릅니다.

다 됐으면 커밋합니다.

```bash
git add .
git commit -m "사이클2: 링크 버튼 섹션 추가 및 내 정보 반영"
```

---

## 5️⃣ 사이클 3 — 스타일 보완 + 다크모드 추가

```
카드 배경색을 그라데이션으로 바꿔줘.
그리고 우측 상단에 다크모드/라이트모드 전환 버튼을 추가해줘.
버튼을 누르면 배경과 글자색이 어두운 테마로 바뀌게 해줘.
```

다크모드 버튼이 생기면, `script.js` 파일을 열어서 `click`이라는 단어가 들어간 부분을 찾아보세요. 버튼을 "클릭했을 때" 어떤 동작이 일어나도록 연결되어 있는 부분입니다.

- [ ] 버튼 클릭 → 색상이 바뀌는 흐름을 내 말로 한 번 설명해보기

원하는 만큼 색상, 폰트, 아이콘 등을 자유롭게 더 보완해도 좋습니다. 다 됐으면 커밋합니다.

```bash
git add .
git commit -m "사이클3: 스타일 보완 및 다크모드 토글 추가"
```

---

## 6️⃣ 내가 만든 코드, AI가 설명해주는 학습 문서 만들기

`learn` 폴더를 만들고, Gemini에게 완성된 코드 전체를 보여주며 설명을 요청합니다.

```bash
mkdir learn
```

```
아래는 내가 만든 프로필 카드의 HTML/CSS/JS 코드야.
각 코드가 무엇을 하는지 초보자도 이해할 수 있게 설명하는 마크다운 문서를
HTML용, CSS용, JS용으로 각각 나눠서 작성해줘.

[index.html 코드 전체 붙여넣기]
[style.css 코드 전체 붙여넣기]
[script.js 코드 전체 붙여넣기]
```

Gemini가 준 답변을 아래처럼 각각 파일로 저장합니다.

- `learn/html.md`
- `learn/css.md`
- `learn/javascript.md`

한 번씩 읽어보고, 내가 만든 코드와 실제로 맞는 설명인지 훑어봅니다. 다 됐으면 커밋합니다.

```bash
git add .
git commit -m "learn 문서 추가"
```

---

## 7️⃣ 배포하기 (dev → gh-pages)

지금까지 작업한 `dev`를 먼저 원격 저장소에 올립니다.

```bash
git push origin dev
```

이제 배포 전용 브랜치 `gh-pages`를 만들고, `dev`의 내용을 그대로 합칩니다.

```bash
git checkout -b gh-pages
git merge dev
git push origin gh-pages
```

GitHub 저장소 페이지에서:

1. **Settings** → **Pages**
2. **Branch**를 `gh-pages`로 선택 → **Save**
3. 잠시 후 표시되는 주소로 접속해서 프로필 카드가 잘 보이는지 확인

---

## 8️⃣ 버전 이름표(태그) 붙이고 Release Note 쓰기

```bash
git tag v1.0
git push origin v1.0
```

GitHub 저장소 페이지 → **Releases** → **Draft a new release**

- Tag: `v1.0` 선택
- 제목: 자유롭게 (예: `프로필 카드 첫 배포`)
- 설명: 오늘 만든 내용을 간단히 적기 (예: "소개부, 링크 버튼, 다크모드 기능을 포함한 첫 배포입니다.")
- **Publish release** 클릭

> 💡 태그는 "이 시점의 코드에 이름표를 붙이는 것", Release Note는 "그 이름표에 대한 설명"입니다. 나중에 프로필 카드를 계속 발전시키게 되면 `v1.1`, `v2.0`처럼 새 태그를 붙여나갈 수 있습니다.

---

## 9️⃣ 조직 저장소로 제출 (Pull Request)

이번엔 `gh-pages`가 아니라 **`dev` 브랜치로** PR을 보냅니다. `dev`에는 오늘 작업한 사이클 1~3의 커밋 기록이 전부 남아있기 때문입니다.

1. 본인 저장소에서 브랜치를 `dev`로 전환한 상태에서 **Contribute** → **Open pull request**
2. 원본 저장소(`hitech26-week3-profilecard`)의 `main` 브랜치로, 내 `dev` 브랜치를 PR
3. 제목에 이름 포함 (예: `[홍길동] 프로필 카드 제출`)

> 💡 이 PR도 실제로 Merge되지 않는 제출용입니다.

---

## ✅ 오늘 완성 체크

- [ ] `dev` 브랜치에서 사이클 1~3을 모두 커밋했다
- [ ] 프로필 이미지, 이름, SNS 링크를 내 정보로 직접 수정했다
- [ ] 다크모드 토글이 동작한다
- [ ] `learn/html.md`, `learn/css.md`, `learn/javascript.md` 3개 파일이 있다
- [ ] `gh-pages` 브랜치로 배포해서 실제 링크로 접속되는 것을 확인했다
- [ ] `v1.0` 태그와 Release Note를 작성했다
- [ ] `dev` 브랜치로 조직 저장소에 PR을 제출했다

---

## ❓ 자주 묻는 질문 FAQ

### Q1. 지금 내가 어느 브랜치에 있는지 어떻게 확인하나요?
**A**: 터미널에 아래 명령어를 입력하면 확인할 수 있습니다.

```bash
git branch
```

별표(`*`)가 붙어있는 브랜치가 지금 작업 중인 브랜치입니다. VS Code 좌측 하단에도 항상 표시됩니다.

### Q2. `dev`에서 `gh-pages`로 옮길 때 충돌(conflict)이 났어요
**A**: `gh-pages`를 방금 새로 만든 거라면 보통 충돌이 날 이유가 없습니다. `git branch`로 지금 정말 `gh-pages`에 있는지, `git merge dev`를 제대로 입력했는지 먼저 확인해보세요.

### Q3. GitHub Pages 설정에서 `gh-pages` 브랜치가 목록에 안 보여요
**A**: `git push origin gh-pages`를 먼저 해야 원격 저장소에 그 브랜치가 생기고, 그래야 Pages 설정 화면에 나타납니다.

### Q4. 태그 push하는 걸 깜빡했어요
**A**: 나중에라도 아래 명령어로 한 번에 올릴 수 있습니다.

```bash
git push origin --tags
```

### Q5. Releases 메뉴를 못 찾겠어요
**A**: 저장소 메인 페이지 우측 사이드바에 "Releases"라는 항목이 있습니다. 없다면 저장소 상단 탭에서 찾아보세요.

---

**📚 수고하셨습니다! 완성된 프로필 카드는 8주차 포트폴리오 허브에서 다시 사용하게 됩니다. 다음 주부터는 배포 방식이 다시 Netlify로 바뀝니다.**
